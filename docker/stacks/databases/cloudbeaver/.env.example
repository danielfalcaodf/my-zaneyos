# No secrets needed at startup — admin password set via first-run wizard.
# CB_SERVER_URL is optional, used when behind a reverse proxy.
CB_SERVER_URL=http://cloudbeaver.localhost
CLOUDBEAVER_PORT=8979
