# System-level edition modules entrypoint.
# Imported from modules/core/default.nix based on variables.nix `edition` field.
#
# Each edition file (vm.nix, basic.nix, medium.nix, full.nix) is a NixOS module
# that enables/disables system services, packages and features.
